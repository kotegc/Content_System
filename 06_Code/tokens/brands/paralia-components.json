{
  "_doc": "Paralia brand component rules. Edit here, run compile.js to regenerate compiled/paralia-components.css.",
  "brand": "paralia",
  "components": {
    "button": {
      "_doc": "Paralia buttons are pill-shaped, matching the chip toggle and brand aesthetic. Padding is em-based so height scales proportionally if --btn-font-size is overridden to a different type tier.",
      "radius":       "var(--radius-pill)",
      "padding-x":    "1.25em",
      "padding-y":    "0.7em",
      "font-weight":  "500",
      "font-size":    "var(--font-size-body)",
      "font-family":  "var(--font-ui)"
    },
    "chip": {
      "_doc": "Chip toggles are pill-shaped in Paralia. Smaller than buttons by default (font-size-sm).",
      "radius":      "var(--radius-pill)",
      "font-size":   "var(--font-size-sm)",
      "padding-x":   "0.9em",
      "padding-y":   "0.5em",
      "font-family": "var(--font-ui)"
    },
    "slider": {
      "_doc": "Slider track is thin (3px), thumb uses --color-signal (orange).",
      "track-height": "3px",
      "thumb-size":   "14px"
    },
    "input": {
      "_doc": "Inputs are pill-shaped in Paralia. Padding is em-based, matching button proportions.",
      "radius":      "var(--radius-pill)",
      "padding-x":   "0.75em",
      "padding-y":   "0.6em",
      "font-size":   "var(--font-size-body)",
      "font-family": "var(--font-ui)"
    },
    "badge": {
      "_doc": "Badges are small and sharp-cornered for data density. Px-based (not a scalable pill control).",
      "radius":      "var(--radius-sm)",
      "font-size":   "var(--font-size-sm)",
      "padding-x":   "8px",
      "padding-y":   "3px"
    },
    "card": {
      "_doc": "Cards and panel islands use large rounding in Paralia.",
      "radius":      "var(--radius-lg)",
      "padding":     "var(--space-lg)"
    },
    "loader": {
      "_doc": "Thin animated progress bar used on loading/transition screens.",
      "bar-height": "3px",
      "bar-width":  "160px",
      "color":      "var(--color-accent)"
    },
    "shelf": {
      "_doc": "App-launcher tray for the /apps page. Bg/border/text/label-color are literal dark-theme hex values (same constants as --color-panel/--color-rule/--color-text/--color-muted's dark-root values in paralia.json), not var(--color-*) references — those primitives flip under body.light and would wash the shelf out on light-themed pages.",
      "radius":            "var(--radius-lg)",
      "padding-x":         "var(--space-lg)",
      "padding-y":         "var(--space-md)",
      "gap":               "var(--space-xl)",
      "bg":                "#111720",
      "border":            "#232b3b",
      "text":              "#e5ebf2",
      "label-color":       "#8394a8",
      "tile-size":         "56px",
      "icon-size":         "30px",
      "label-font-size":   "var(--font-size-sm)",
      "inactive-opacity":  "0.4"
    }
  }
}
